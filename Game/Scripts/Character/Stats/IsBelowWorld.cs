
using UnityEngine;

namespace Game
{
    public class IsPlayerBelowWorld : MonoBehaviour
    {
        /*
        [SerializeField] private Rigidbody playerRigidBody; // Reference to the playerRigidBody component
        [SerializeField] private Vector3 respawnAt = new Vector3(0f, 100f, 0f); // Height at which the player will respawn if they fall below the world

        private static event System.Action RespawnPlayerAbove; // Event to respawn the player above the world
        private static event System.Action<float> RigidBodyYPosition; 

        private void IfBelowWorld()
        {

        //    RigidBodyYPosition?.Invoke(playerRigidBody.position.y); // Invoke the event to check the Y position of the rigid body
            if (playerRigidBody.position.y <= -1000f)
            {

            //    YPositionBelowWorld.Invoke(rigidBody.RigidBody.position.y);
            } // Respawn the player above the world if they fall below a certain height
        }
        */
    }
}